# ADK Playground Requirements
# Install with: pip install -r requirements.txt
# Or use: pip install -e . (which reads from pyproject.toml)

fastapi>=0.109.0
uvicorn>=0.27.0
pyyaml>=6.0
pydantic>=2.0
python-multipart>=0.0.6
websockets>=12.0
google-adk

