"""In-app ADK agents and multi-agent systems.

Each module here should expose builders that return an ADK `Agent` instance
configured for a specific task (prompt generation, tool generation, etc.).
"""

